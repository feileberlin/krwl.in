# ==============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env for local development
# 
# AUTOMATIC BEHAVIOR:
# - Leave empty for development mode (debug=true, demo data=true)
# - CI environments automatically disable debug and demo data
# - Production requires NODE_ENV=production
# ==============================================================================

# Environment setting
# Typically leave this commented out - auto-detects as 'development' by default
# Uncomment only if you want to force a specific environment:
# NODE_ENV=development
# NODE_ENV=production

# CI Detection (automatically set by CI providers like GitHub Actions)
# CI=true
# GITHUB_ACTIONS=true
