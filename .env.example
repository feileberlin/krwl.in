# ==============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env for local development
# 
# AUTOMATIC BEHAVIOR:
# - Leave empty for development mode (debug=true, demo data=true)
# - CI environments automatically detected (GitHub Actions, GitLab, Travis, etc.)
# - Production hosting automatically detected (Vercel, Netlify, Heroku, Railway, 
#   Render, Fly.io, Google Cloud Run, AWS, etc.)
# - Most hosting platforms set their own environment variables - no manual config needed!
# ==============================================================================

# Environment setting (OPTIONAL - only if you need to force a specific mode)
# Typically leave this commented out - auto-detects correctly for most platforms
# 
# Uncomment only if you want to force a specific environment:
# NODE_ENV=development
# NODE_ENV=production

# CI Detection (automatically set by CI providers - no need to set manually)
# CI=true                    # Generic CI flag
# GITHUB_ACTIONS=true        # GitHub Actions
# GITLAB_CI=true            # GitLab CI
# TRAVIS=true               # Travis CI
# CIRCLECI=true             # CircleCI

# Hosting Platform Detection (automatically set by platforms - no need to set manually)
# VERCEL_ENV=production     # Vercel
# NETLIFY=true              # Netlify
# CONTEXT=production        # Netlify environment
# DYNO=web.1                # Heroku
# RAILWAY_ENVIRONMENT=production  # Railway
# RENDER=true               # Render
# FLY_APP_NAME=myapp        # Fly.io
# K_SERVICE=myservice       # Google Cloud Run
# AWS_EXECUTION_ENV=AWS_ECS_FARGATE  # AWS
