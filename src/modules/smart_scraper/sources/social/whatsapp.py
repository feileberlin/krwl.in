"""WhatsApp scraper (placeholder)."""

from typing import Dict, Any, List
from ...base import BaseSource, SourceOptions


class WhatsAppSource(BaseSource):
    """WhatsApp public channel events scraper (placeholder)."""
    
    def scrape(self) -> List[Dict[str, Any]]:
        """Scrape events from WhatsApp public channels."""
        print(f"    ⚠ WhatsApp scraping not yet implemented")
        print(f"    → Requires WhatsApp Business API")
        return []
