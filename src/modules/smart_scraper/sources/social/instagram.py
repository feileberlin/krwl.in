"""Instagram scraper (placeholder)."""

from typing import Dict, Any, List
from ...base import BaseSource, SourceOptions


class InstagramSource(BaseSource):
    """Instagram events scraper (placeholder)."""
    
    def scrape(self) -> List[Dict[str, Any]]:
        """Scrape events from Instagram."""
        print(f"    ⚠ Instagram scraping not yet implemented")
        print(f"    → Requires Instagram API or third-party library")
        return []
