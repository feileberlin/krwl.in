"""TikTok scraper (placeholder)."""

from typing import Dict, Any, List
from ...base import BaseSource, SourceOptions


class TikTokSource(BaseSource):
    """TikTok events scraper (placeholder)."""
    
    def scrape(self) -> List[Dict[str, Any]]:
        """Scrape events from TikTok."""
        print(f"    ⚠ TikTok scraping not yet implemented")
        print(f"    → Requires TikTok API or third-party library")
        return []
