# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environment
venv/
.venv/

# IDE - Track shared configs, ignore user-specific settings
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
.cache/

# Hosting-specific gitignore (used on production servers)
.gitignore.hosting

# Platform-specific deployment configs (use examples in hosting-examples/)
# These are gitignored by default to prevent committing secrets or deployment-specific settings
# Copy from hosting-examples/*.example if you need them
vercel.json
netlify.toml
Procfile
railway.json
render.yaml
fly.toml
app.yaml
cloudbuild.yaml
staticwebapp.config.json
Dockerfile
.dockerignore

# Server identity files
.server-id
.deployment-id

# Test and report artifacts
kiss_results.json
kiss_report.txt
.scrape_status

# Deployment artifacts
publish/

# Old documentation (README.md is auto-generated)
README.txt
README.NEW.md
IMPLEMENTATION_SUMMARY.md
PR_SUMMARY.md
KISS_IMPLEMENTATION.md
static/css/style.css.backup
static/logo.svg

# Build artifacts - dependencies are fetched at build time
lib/leaflet/
lib/lucide/
lib/roboto/
lib/roboto-mono/
!lib/versions.json
public/index.html

# WCAG protocol (auto-generated during build)
public/wcag_protocol.txt

# Event data - runtime generated files (exclude from version control)
# These files are generated by scraping and editorial workflows
data/*.json
!data/*.template.json
!data/i18n/*.json

# Weather cache - regenerated hourly
assets/json/weather_cache.json

# Log files
logs/
*.log

# Legacy/backup files - archived in /archive/ directory
# Prevent new backup files from being created
*.backup
*-old.*
*-original.*
*-before-*
*-refactored.*
*_OLD.*
*_BACKUP.*

# Keep archive directory but ignore any future additions
archive/**/new_backups/
